[pytest]
# Run pytest from the backend/ directory so imports match production layout
# (`from services.x import ...`, `from routers.x import ...`).
pythonpath = .
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning:google.*
