rules_version='2'

// All Firestore access goes through the FastAPI backend using the Admin SDK,
// which bypasses these rules. Client-SDK access is denied.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
